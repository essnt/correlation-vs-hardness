# Verified environment: Python 3.14, Linux x86-64 (WSL2, CPU-only). The project
# itself requires Python >= 3.11 (see pyproject.toml) and runs on Linux / macOS /
# Windows (README.md, platform notes). Version pins below are the exact set
# verified in the authors' environment; on other platforms or Python versions,
# adjust pins if a matching wheel is unavailable.
# pymupdf: used by scripts/final_verify.py (paper PDF checks);
# python-igraph + leidenalg: community metrics in src/cvh/metrics.py
# (modularity / mediation dependencies, lazily imported)
lifelines==0.30.3
matplotlib==3.11.1
networkx==3.6.1
numpy==2.5.3
pandas==2.3.3
pytest==9.1.1
pymupdf==1.28.2
python-igraph==1.0.0
python-sat==1.9.dev15
leidenalg==0.12.0
scikit-learn==1.9.0
scipy==1.18.1
statsmodels==0.15.0
